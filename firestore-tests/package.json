{
  "name": "villamor-crm-firestore-tests",
  "version": "1.0.0",
  "description": "Testes das Firestore Security Rules do Villamor CRM",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
